// === backend/src/sockets/trip.socket.js ===
// Purpose: Socket.io event handlers for trip room management
// Dependencies: ../services/tracking.service

/**
 * TODO: Implement registerTripSocket(io, socket)
 *
 * Events:
 *   'trip:join' — { tripId } → trackingService.joinTripRoom(socket.id, tripId, socket.userId)
 *   'trip:leave' — { tripId } → socket.leave('trip:' + tripId)
 *   'trip:stop:complete' — { tripId, stopId } → trackingService.completeStop(...)
 */

// function registerTripSocket(io, socket) { /* TODO */ }
// module.exports = { registerTripSocket };
