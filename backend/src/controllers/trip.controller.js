// === backend/src/controllers/trip.controller.js ===
// Purpose: Trip request handlers — parse req, call service, send response
// Dependencies: ../services/trip.service

// const service = require('../services/trip.service');

/**
 * TODO: Implement controller methods: getAll, getById, start, completeStop, getRoute
 *
 * Pattern for each method:
 *   exports.methodName = async (req, res, next) => {
 *     try {
 *       const result = await service.methodName(req.body, req.user);
 *       res.status(200).json(result);
 *     } catch (error) {
 *       next(error);
 *     }
 *   };
 *
 * Called by: ../routes/trip.routes.js
 * Calls: ../services/trip.service
 */
