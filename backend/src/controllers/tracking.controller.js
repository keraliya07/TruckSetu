// === backend/src/controllers/tracking.controller.js ===
// Purpose: Tracking request handlers — parse req, call service, send response
// Dependencies: ../services/tracking.service

// const service = require('../services/tracking.service');

/**
 * TODO: Implement controller methods: getLatestLocation, getLocationHistory
 *
 * Pattern for each method:
 *   exports.methodName = async (req, res, next) => {
 *     try {
 *       const result = await service.methodName(req.body, req.user);
 *       res.status(200).json(result);
 *     } catch (error) {
 *       next(error);
 *     }
 *   };
 *
 * Called by: ../routes/tracking.routes.js
 * Calls: ../services/tracking.service
 */
