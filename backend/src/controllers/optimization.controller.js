// === backend/src/controllers/optimization.controller.js ===
// Purpose: Optimization request handlers — parse req, call service, send response
// Dependencies: ../services/optimization.service

// const service = require('../services/optimization.service');

/**
 * TODO: Implement controller methods: scoreTrucks, getCachedResult, truckFitEstimate
 *
 * Pattern for each method:
 *   exports.methodName = async (req, res, next) => {
 *     try {
 *       const result = await service.methodName(req.body, req.user);
 *       res.status(200).json(result);
 *     } catch (error) {
 *       next(error);
 *     }
 *   };
 *
 * Called by: ../routes/optimization.routes.js
 * Calls: ../services/optimization.service
 */
