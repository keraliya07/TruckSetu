// === backend/src/validators/auth.validator.js ===
// Purpose: Zod validation schemas for auth request bodies
// Dependencies: zod

// const { z } = require('zod');

/**
 * TODO: Define validation schemas:
 *
 * loginSchema: { email: z.string().email(), password: z.string().min(6) }
registerSchema: { email, password, name, phone?, role: z.enum(['WAREHOUSE','DEALER']) }
 *
 * Export: module.exports = { ...schemas };
 */
