// === backend/src/routes/optimization.routes.js ===
// Purpose: Optimization route definitions
// Dependencies: express.Router, ../controllers/optimization.controller, ../middleware/*

// const router = require('express').Router();
// const controller = require('../controllers/optimization.controller');
// const { requireRole } = require('../middleware/role.middleware');
// const { validate } = require('../middleware/validate.middleware');

/**
 * TODO: Define route endpoints:
 * POST /score, GET /result/:cacheKey, POST /truck-fit
 *
 * Apply middleware chain:
 *   - requireRole() for role-restricted routes
 *   - validate(schema) for request body validation
 *
 * Example:
 *   router.post('/', requireRole('WAREHOUSE'), validate(createSchema), controller.create);
 */

// module.exports = router;
