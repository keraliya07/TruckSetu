// === backend/src/validators/booking.validator.js ===
// Purpose: Zod validation schemas for booking request bodies
// Dependencies: zod

// const { z } = require('zod');

/**
 * TODO: Define validation schemas:
 *
 * createBookingSchema: { shipmentIds: z.array(z.string()), truckId, quotedPrice }
respondSchema: { action: z.enum(['APPROVE','REJECT','COUNTER']), counterPrice?, dealerNote? }
 *
 * Export: module.exports = { ...schemas };
 */
