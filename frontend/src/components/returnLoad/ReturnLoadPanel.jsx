// === frontend/src/components/returnLoad/ReturnLoadPanel.jsx ===
// Purpose: Panel listing all return load matches for a completed trip
// Dependencies: ./ReturnLoadCard, ../common/EmptyState, ../common/LoadingSpinner

/**
 * TODO: Implement ReturnLoadPanel component
 *
 * Props:
 *   @param {array} matches — Array of return load match objects
 *   @param {boolean} isLoading
 *   @param {function} onAccept — Accept handler per match
 *   @param {function} onReject — Reject handler per match
 *
 * Display:
 *   - Header: "Return Load Opportunities (3 matches)"
 *   - List of ReturnLoadCard components sorted by combinedScore desc
 *   - If no matches: EmptyState "No return loads available near your destination"
 *
 * @returns {JSX.Element}
 */

// export default function ReturnLoadPanel({ matches, isLoading, onAccept, onReject }) {
//   // TODO: Implement return load panel
// }
