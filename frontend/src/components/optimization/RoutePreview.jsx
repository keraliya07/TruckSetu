// === frontend/src/components/optimization/RoutePreview.jsx ===
// Purpose: Show proposed stop sequence for a truck before booking
// Dependencies: ../maps/TrackingMap, ../common/StatusBadge

/**
 * TODO: Implement RoutePreview component
 *
 * Purpose: Display the optimized route stops in order with map preview
 *
 * Visual:
 *   Left side: Ordered list of stops with city names, types, and estimated times
 *   Right side: Mini map showing the route polyline and stop markers
 *
 * Props:
 *   @param {array} stops — [{ city, type: 'PICKUP'|'DELIVERY', lat, lng, estimatedTime }]
 *   @param {number} totalDistanceKm
 *   @param {string} estimatedDuration
 *   @param {GeoJSON} routeGeometry — OSRM route for the map
 *
 * @returns {JSX.Element}
 */

// export default function RoutePreview({ stops, totalDistanceKm, estimatedDuration, routeGeometry }) {
//   // TODO: Implement route preview with stop list and map
// }
