// === frontend/src/components/booking/CounterOfferModal.jsx ===
// Purpose: Modal for dealer to submit a counter-offer price with note
// Dependencies: react-hook-form, zod, ../common/ConfirmModal

/**
 * TODO: Implement CounterOfferModal component
 *
 * Purpose: Dealer inputs counter price and optional note
 *
 * Props:
 *   @param {boolean} isOpen
 *   @param {function} onClose
 *   @param {object} booking — Current booking (shows original quoted price)
 *   @param {function} onSubmit — Called with { counterPrice, dealerNote }
 *   @param {boolean} isLoading
 *
 * Form fields:
 *   - Counter Price (₹): number input, min = 0, required
 *   - Note to Warehouse: textarea, optional
 *   - Display: Original quote vs counter offer comparison
 *
 * @returns {JSX.Element}
 */

// export default function CounterOfferModal({ isOpen, onClose, booking, onSubmit, isLoading }) {
//   // TODO: Implement counter offer form modal
// }
