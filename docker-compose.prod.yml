# === docker-compose.prod.yml ===
# Smart Truck Loading Optimization System (STLOS) — Production Overrides
# Dependencies: docker-compose.yml (base config)
#
# USAGE:
#   docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d
#
# TODO: Define production overrides for each service:
#
# ─── node-api ───
# build:
#   target: production        # Multi-stage build, production stage
# environment:
#   NODE_ENV: production
# restart: unless-stopped
# deploy:
#   replicas: 2               # Run 2 instances behind load balancer
#   resources:
#     limits:
#       cpus: '1'
#       memory: 1024M
# # Remove volume mounts (use built image instead)
#
# ─── python-ml ───
# restart: unless-stopped
# deploy:
#   resources:
#     limits:
#       cpus: '2'
#       memory: 2048M         # ML workloads need more memory
#
# ─── frontend ───
# build:
#   target: production        # Serve via nginx in production
# restart: unless-stopped
#
# ─── postgres ───
# restart: unless-stopped
# environment:
#   POSTGRES_PASSWORD: ${DB_PASSWORD}   # Use env var, not hardcoded
# # TODO: Add backup volume and backup cron
#
# ─── redis ───
# restart: unless-stopped
# command: redis-server --maxmemory 256mb --maxmemory-policy allkeys-lru
#
# ─── osrm ───
# restart: unless-stopped
#
# ─── nginx (NEW — add reverse proxy) ───
# image: nginx:alpine
# ports: 80:80, 443:443
# volumes:
#   - ./nginx/nginx.conf:/etc/nginx/nginx.conf
#   - ./nginx/certs:/etc/nginx/certs    # SSL certificates
# depends_on: [frontend, node-api]
